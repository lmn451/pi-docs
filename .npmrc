# 1. Block malicious scripts from running during install
ignore-scripts=true

# 2. Shallow install layout (prevents "phantom" dependencies)
install-strategy=shallow

# 3. Enforce strict versioning and engine checks
engine-strict=true

# 4. Severity threshold reported by `npm audit`
audit-level=high
